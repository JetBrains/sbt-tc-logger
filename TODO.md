# Follow-up notes

- Reassess the retired `TW-50753` initialization-error scenario to determine whether it is still meaningful for supported SBT versions.
- Reconsider using the TeamCity `serviceMessages` API for serialization and standard message types, instead of manually assembling protocol strings; review the benefits and compatibility details before changing the logger.
- fix all the packages, unify it with other tools we have, move files to those packages.
- Remove the header commits with license notes?
