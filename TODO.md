# Follow-up notes

- Reassess the retired `TW-50753` initialization-error scenario to determine whether it is still meaningful for supported SBT versions.
- In a separate commit, rename the `testdata/1.0` and `testdata/2.0` fixture roots to `1.0+` and `2.0+` to make their
  forward-compatibility intent explicit.
